# 阶段 0 风险台账

> 状态：阶段 0 动态证据；不替代 06 清单的业务签字。

| 编号 | 风险 | 信号/证据 | 当前处理 | 退出条件 |
| --- | --- | --- | --- | --- |
| R-001 | MinIO 社区仓库已于 2026-04-25 归档，旧镜像不能安全进入生产 | 官方仓库归档；最后可获取镜像经 Trivy 0.72.0 检出 71 项可修复 HIGH/CRITICAL，含 CRITICAL | 已关闭：2026-07-13 改用 SeaweedFS 4.29 安全派生版；业务层仅依赖 S3 Adapter | 五镜像扫描为 0；版本恢复、短时授权、预览和下载全部重新通过 |
| R-002 | 本地 Windows 未安装 Docker，容器 POC 不能在本机执行 | `docker` 命令不存在 | 已接受：局域网服务器隔离目录和独立 Compose project 是标准容器验证环境，不连接正式目录 | 项目发起人已确认远端隔离验证方式；正式与隔离环境分目录、分端口、分卷 |
| R-003 | 局域网服务器 Docker Hub 端点不能稳定解析官方 Node 24 标签或摘要 | `node:24.18.0-*` 返回 manifest not found | 已关闭：使用按摘要固定的 Red Hat UBI 9 minimal，从 nodejs.org 下载并校验 Node 24.18.0 官方 SHASUM；运行层移除构建工具 | API/Worker 扫描为 0；Node 24 lint、类型、36 项测试、构建和运行全部通过 |
| R-004 | MySQL 默认全文解析器对无空格中文零命中 | 阶段 0 查询“功率芯片”返回 0；官方文档确认默认解析器不能识别中日韩词边界 | 已关闭：使用内置 `ngram`、`ngram_token_size=2` 和受控原生 DDL | 中文查询、结构化筛选、关系扩展和性能基线通过 |
