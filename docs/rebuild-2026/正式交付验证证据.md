# 正式交付验证证据

> 执行日期：2026-07-13。先在局域网服务器隔离目录完成全链路验证，再部署至正式目录 `/opt/xinmaowei`；正式入口为 `http://172.16.172.85:8082`。

## 自动化质量

| 项目 | 结果 |
| --- | --- |
| ESLint | 通过，无禁用规则 |
| TypeScript | client/server 均通过 |
| 服务端 Vitest/Supertest | 14 个文件、32 项通过 |
| 前端 RTL/Vitest | 2 个文件、4 项通过 |
| Playwright E2E | 正式生产桌面与 320px 两项目、2 项通过 |
| 生产构建 | client/server 均通过 |
| 禁止项扫描 | 无 `any`、`@ts-ignore`、`eslint-disable`；无前端假数据 |

## 主链路与授权

| 场景 | 结果 |
| --- | --- |
| 空初始化 | 产品、方案、资料均为 0；仅管理员、RBAC、必要配置 |
| 产品 → 关联方案 → 方案资料 | 受控数据验证通过 |
| 匿名公开 PDF 预览 | 严格 3 页 |
| 注册个人完整预览 | 5 页 |
| 原件下载 | 下载内容与上传原件一致 |
| 配置版本 | 草稿、预览、发布验证通过 |
| break-glass / 审计 / Outbox | 4 / 4 / 未处理 0 |

## 运维、安全与恢复

| 项目 | 结果 |
| --- | --- |
| 产品详情 P95 | 48.93 ms（阈值 1500 ms） |
| 快速选型 P95 | 44.67 ms（阈值 2000 ms） |
| MySQL 全量恢复 | 产品 1、资料版本 1、审计 4，隔离恢复库校验通过 |
| S3 对象版本恢复 | 删除当前版本后按 version ID 恢复 v2，通过 |
| Trivy 0.72.0 | API、Web、MySQL 9.7、Redis、SeaweedFS 五镜像可修复 HIGH/CRITICAL 均为 0 |
| 对象存储供应链 | SeaweedFS 官方 4.29 提交 `1355c7a102194d6c461baf090eff50367b575afb`；源码归档和官方 Go sum 记录固定并校验 |
| 正式生产备份恢复 | `Subject / Role / ConfigurationVersion = 1 / 19 / 1`；业务表均为 0，隔离恢复通过 |
| 旧部署清理 | 旧目录、容器、数据卷、镜像和旧监听端口均已清理 |

## 浏览器验收

- 桌面：找型号 → 产品详情 → 方案详情 → 资料入口通过。
- 移动端：320px `clientWidth = scrollWidth = 320`，无横向阻塞。
- 200% 等效视口：`clientWidth = scrollWidth = 720`，无横向阻塞。
- 键盘：Tab 聚焦跳转链接，Enter 到达 `main-content`。
- 控制台：0 error、0 warning。
- 注册后可直接进入个人工作台；不强制建企业、项目；无项目支持工单提交通过。

截图位于 `output/playwright/`，包含受控主链路、客户协作工作台以及最终生产桌面和 320px 页面。

## 可重复命令

```powershell
python scripts/remote_validate.py
python scripts/remote_scan_images.py
python scripts/remote_operational_verify.py
python scripts/remote_verify_production.py
$env:E2E_BASE_URL="http://172.16.172.85:8082"; pnpm e2e
```
