# 阶段 0 验证证据

> 初次执行日期：2026-07-12；正式复验日期：2026-07-13。环境：本地 Windows + 局域网服务器隔离目录。本文件只记录实际执行结果，未执行项不视为通过。

## 已通过

| 验证项 | 结果 | 证据 |
| --- | --- | --- |
| Node 24 | 通过 | 本地与远端 runner 均输出 `Node.js v24.18.0 runtime POC passed` |
| Prisma/MySQL schema | 通过 | Prisma 7.8.0 validate 与 MySQL 9.7.0 `db push` 成功 |
| MySQL 数据能力 | 通过 | 主外键、JSON、事务提交/回滚、产品详情/料号、方案适配和审计查询通过 |
| 中文全文 | 通过（修订后） | 默认解析器零命中；改用 `WITH PARSER ngram`、`ngram_token_size=2` 后“功率芯片”命中，结论已回写技术基线 |
| 干净初始化 | 通过 | 初始化仅保留阶段 0 管理员、系统角色和六类字段定义；产品、方案、资料数量均为 0 |
| 文件与资源授权 | 通过 | Vitest 4 个文件、16 项测试通过：资源等级、匿名前三页、注册完整预览、短时令牌、防篡改、过期拒绝、全包过滤、Outbox 幂等/失败/超时、Office Adapter 排队状态 |
| PDF 派生物 | 通过 | 实际生成 5 页原件与 3 页匿名派生 PDF，页数和输入 SHA-256 版本键核对通过；当前仓库未提供真实 Office 样本，因此按修订门槛验证 Adapter 边界，不伪造 Office 转换成功 |
| Redis | 通过 | `redis-cli ping` 返回 `PONG` |
| S3 对象版本恢复 | 通过 | 正式存储切换为 SeaweedFS 4.29 安全派生版；同一对象写入 v1/v2、删除当前版本、按 version ID 读取 v2 并恢复为当前版本 |
| MySQL 备份与 PITR | 通过 | 全量备份恢复后管理员数量为 1；binlog 起止位点恢复结果严格为 `baseline,desired`，截止点后的 `unwanted` 未恢复 |
| 服务回滚 | 通过 | Compose 发布 API v2 后回滚到 v1，健康响应的 `release` 从 `v2` 恢复为 `v1` |
| API/Worker/Nginx | 通过 | Nginx→API 统一响应 `code=0`；Worker 对 MySQL/Redis/S3 依赖通过；JSON 日志含 service/trace ID；资源限制生效 |
| 容器健康与端口边界 | 通过 | API、Worker、Nginx、MySQL、Redis、SeaweedFS 均为 healthy；生产仅 Web 暴露宿主端口 |
| Playwright 体验原型 | 通过 | 控制台 0 错误/0 警告；320px 和 200% 等效视口无横向页面溢出；跳转链接、对话框焦点与 Escape 恢复通过；见下方截图 |

## 正式 Playwright 截图

- [桌面主链路](../../output/playwright/formal-desktop-main-flow.png)
- [移动端 320](../../output/playwright/formal-mobile-320.png)
- [200% 缩放等效视口](../../output/playwright/formal-zoom-200-equivalent.png)
- [客户协作工作台](../../output/playwright/formal-customer-workspace.png)

## 阶段 0 退出结论

阶段 0 POC、A-D 确认和自动化证据已于 2026-07-12 完成。2026-07-13 已用正式实现重新通过文件授权、对象版本恢复、Node 24 全套质量检查和镜像扫描；R-001、R-003、R-004 已满足退出条件。

## 可重复命令

```powershell
python scripts/remote_validate.py
python scripts/remote_scan_images.py
python scripts/remote_operational_verify.py
```

完成标志依次为 `REMOTE_FORMAL_VALIDATION=PASSED`、`FORMAL_IMAGE_SCAN=PASSED`、`REMOTE_OPERATIONAL_VALIDATION=PASSED`。
